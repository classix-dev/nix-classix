--- a/apps/web/src/components/common/Header/index.tsx
+++ b/apps/web/src/components/common/Header/index.tsx
@@ -83,14 +83,16 @@
       </div>
 
       <div className={classnames(css.element, css.logoMobile)}>
-        <Link href={logoHref} passHref>
-          {isOfficialHost ? <SafeLogoMobile alt="Safe logo" /> : null}
+        <Link href={logoHref} passHref className={css.classixWordmark}>
+          <div className={css.classixName}>{BRAND_NAME}</div>
+          <div className={css.classixEdition}>{process.env.NEXT_PUBLIC_CLASSIX_TAGLINE || ''}</div>
         </Link>
       </div>
 
       <div className={classnames(css.element, css.hideMobile, css.logo)}>
-        <Link href={logoHref} passHref>
-          {isOfficialHost ? <SafeLabsLogo alt={BRAND_NAME} /> : BRAND_LOGO && <img src={BRAND_LOGO} alt={BRAND_NAME} />}
+        <Link href={logoHref} passHref className={css.classixWordmark}>
+          <div className={css.classixName}>{BRAND_NAME}</div>
+          <div className={css.classixEdition}>{process.env.NEXT_PUBLIC_CLASSIX_TAGLINE || ''}</div>
         </Link>
       </div>
 
--- a/apps/web/src/components/common/Header/styles.module.css
+++ b/apps/web/src/components/common/Header/styles.module.css
@@ -60,6 +60,70 @@
   color: var(--color-logo-main);
 }
 
+/* The wordmark is a flex item of `.element.logo` (column flex,
+   align-items: flex-start). Block children inside that container
+   collapse to 0×0 due to MUI's CssBaseline cascade. We:
+   - `align-self: stretch` to fill the slot's horizontal cross-axis
+   - make the wordmark itself a flex column so vertical sizing is
+     governed by `min-height: max-content` on the children
+   - set explicit pixel heights on each line so text always paints */
+.classixWordmark {
+  display: flex;
+  flex-direction: column;
+  justify-content: center;
+  align-items: stretch;
+  align-self: stretch;
+  gap: 2px;
+  text-decoration: none;
+  padding: 0;
+}
+
+.classixName,
+.classixEdition {
+  display: block;
+  white-space: nowrap;
+  margin: 0;
+  padding: 0;
+  flex: 0 0 auto;
+  width: auto;
+  min-width: max-content;
+  min-height: max-content;
+}
+
+.classixName {
+  font-family: 'Space Grotesk', system-ui, sans-serif;
+  font-weight: 600;
+  font-size: 16px;
+  line-height: 18px;
+  height: 18px;
+  letter-spacing: -0.005em;
+  color: var(--color-text-primary);
+}
+
+.classixEdition {
+  font-family: 'Michroma', system-ui, sans-serif;
+  font-weight: 400;
+  font-size: 12px;
+  line-height: 14px;
+  height: 14px;
+  letter-spacing: 0.12em;
+  text-transform: lowercase;
+  color: #12ff80;
+}
+
+@media (max-width: 899.95px) {
+  .classixName {
+    font-size: 14px;
+    line-height: 16px;
+    height: 16px;
+  }
+  .classixEdition {
+    font-size: 11px;
+    line-height: 13px;
+    height: 13px;
+  }
+}
+
 .logo {
   padding: var(--space-1) var(--space-2);
 }
--- a/apps/web/src/components/common/MetaTags/index.tsx
+++ b/apps/web/src/components/common/MetaTags/index.tsx
@@ -37,12 +37,13 @@
     <meta name="theme-color" content={darkPalette.background.main} media="(prefers-color-scheme: dark)" />
     <link rel="manifest" href="/safe.webmanifest" {...(IS_BEHIND_IAP && { crossOrigin: 'use-credentials' })} />
 
-    {/* Favicons */}
-    <link rel="shortcut icon" href="/favicons/favicon.ico" />
-    <link rel="apple-touch-icon" sizes="180x180" href="/favicons/apple-touch-icon.png" />
-    <link rel="icon" type="image/png" sizes="32x32" href="/favicons/favicon-32x32.png" />
-    <link rel="icon" type="image/png" sizes="16x16" href="/favicons/favicon-16x16.png" />
-    <link rel="mask-icon" href="/favicons/safari-pinned-tab.svg" color="#000" />
+    {/* Favicon. Classix deploys ship a single SVG icon — the chain
+        diamond, scales at any DPR. The upstream's raster fallbacks
+        (favicon-16x16.png, favicon.ico, apple-touch-icon.png) were
+        dropped on purpose: when both an SVG and rasters are advertised
+        in <head>, some browsers cache-pin the raster and ignore later
+        SVG updates. Single source of truth keeps re-deploys honest. */}
+    <link rel="icon" type="image/svg+xml" href="/favicons/icon.svg" />
   </>
 )
 
--- a/apps/web/src/components/common/Footer/index.tsx
+++ b/apps/web/src/components/common/Footer/index.tsx
@@ -13,6 +13,18 @@
 import { IS_PRODUCTION, COMMIT_HASH } from '@/config/constants'
 import type { FooterProps } from './footer.type'
 
+type ClassixFooterLink = { label: string; url: string }
+const parseClassixLinks = (raw: string | undefined): ClassixFooterLink[] => {
+  if (!raw) return []
+  try {
+    const v = JSON.parse(raw)
+    return Array.isArray(v) ? v.filter((l) => l && l.label && l.url) : []
+  } catch {
+    return []
+  }
+}
+const CLASSIX_FOOTER_LINKS = parseClassixLinks(process.env.NEXT_PUBLIC_CLASSIX_FOOTER_LINKS)
+
 const footerPages = [
   AppRoutes.settings.index,
   AppRoutes.imprint,
@@ -90,11 +102,30 @@
             )}
           </>
         ) : (
-          <li>This is an unofficial distribution of the app</li>
+          CLASSIX_FOOTER_LINKS.length > 0 ? (
+            <>
+              {CLASSIX_FOOTER_LINKS.map((l) => (
+                <li key={l.url}>
+                  <ExternalLink href={l.url} noIcon>
+                    {l.label}
+                  </ExternalLink>
+                </li>
+              ))}
+            </>
+          ) : (
+            <li>This is an unofficial distribution of the app</li>
+          )
         )}
 
         <li>
-          <ExternalLink href={`${APP_HOMEPAGE}/releases/tag/web-v${APP_VERSION}`} noIcon>
+          <ExternalLink
+            href={
+              process.env.NEXT_PUBLIC_CLASSIX_GITHUB_REPO
+                ? process.env.NEXT_PUBLIC_CLASSIX_GITHUB_REPO
+                : `${APP_HOMEPAGE}/releases/tag/web-v${APP_VERSION}`
+            }
+            noIcon
+          >
             {versionIcon && <SvgIcon component={GitHubIcon} inheritViewBox fontSize="inherit" sx={{ mr: 0.5 }} />}v
             {APP_VERSION}
           </ExternalLink>
--- a/apps/web/src/components/common/PageLayout/index.tsx
+++ b/apps/web/src/components/common/PageLayout/index.tsx
@@ -1,6 +1,20 @@
 import { useContext, useEffect, useState, type ReactElement } from 'react'
 import classnames from 'classnames'
 import { AnimatePresence, motion } from 'motion/react'
+import Button from '@mui/material/Button'
+import Dialog from '@mui/material/Dialog'
+import DialogActions from '@mui/material/DialogActions'
+import DialogContent from '@mui/material/DialogContent'
+import DialogContentText from '@mui/material/DialogContentText'
+import DialogTitle from '@mui/material/DialogTitle'
+
+const CLASSIX_NOTIFICATION = process.env.NEXT_PUBLIC_CLASSIX_NOTIFICATION || ''
+const CLASSIX_NOTIFICATION_ACK_KEY = 'classix:notification:ack'
+// Re-show the notification modal once an hour. The most recent ack
+// timestamp lives in localStorage; a 1-min poll re-evaluates so a
+// user who never reloads still sees the modal again on schedule.
+const CLASSIX_NOTIFICATION_INTERVAL_MS = 60 * 60 * 1000
+const CLASSIX_NOTIFICATION_POLL_MS = 60 * 1000
 
 import Topbar from '@/components/common/Header/Topbar'
 import Header from '@/components/common/Header'
@@ -59,8 +73,62 @@
     setFullWidth(!isSidebarVisible)
   }, [isSidebarVisible, setFullWidth])
 
+  // Classix notification: full-screen modal that re-fires every
+  // CLASSIX_NOTIFICATION_INTERVAL_MS. localStorage holds the last ack
+  // timestamp; we re-evaluate on mount and on a 1-min poll so a user
+  // who never reloads still sees the modal again on schedule.
+  const [classixNotificationOpen, setClassixNotificationOpen] = useState(false)
+  useEffect(() => {
+    if (!CLASSIX_NOTIFICATION) return
+    if (typeof window === 'undefined') return
+    const evaluate = () => {
+      let lastAck = 0
+      try {
+        const raw = window.localStorage.getItem(CLASSIX_NOTIFICATION_ACK_KEY)
+        const parsed = raw ? Number(raw) : 0
+        lastAck = Number.isFinite(parsed) && parsed > 0 ? parsed : 0
+      } catch {
+        // localStorage may throw in private mode / disabled — treat as never acked
+      }
+      if (Date.now() - lastAck >= CLASSIX_NOTIFICATION_INTERVAL_MS) {
+        setClassixNotificationOpen(true)
+      }
+    }
+    evaluate()
+    const id = window.setInterval(evaluate, CLASSIX_NOTIFICATION_POLL_MS)
+    return () => window.clearInterval(id)
+  }, [])
+  const handleClassixNotificationAck = () => {
+    setClassixNotificationOpen(false)
+    try {
+      window.localStorage.setItem(CLASSIX_NOTIFICATION_ACK_KEY, String(Date.now()))
+    } catch {
+      // ignore — ack not persisted, but the user has seen it this load
+    }
+  }
+
   return (
     <>
+      {CLASSIX_NOTIFICATION && (
+        <Dialog
+          open={classixNotificationOpen}
+          aria-labelledby="classix-notification-title"
+          aria-describedby="classix-notification-text"
+          maxWidth="xs"
+          fullWidth
+        >
+          <DialogTitle id="classix-notification-title">Heads up</DialogTitle>
+          <DialogContent>
+            <DialogContentText id="classix-notification-text">{CLASSIX_NOTIFICATION}</DialogContentText>
+          </DialogContent>
+          <DialogActions>
+            <Button onClick={handleClassixNotificationAck} autoFocus variant="contained">
+              OK
+            </Button>
+          </DialogActions>
+        </Dialog>
+      )}
+
       {!hideHeader && isSpaceRoute && (
         <div className={css.topbar}>
           <Topbar onMenuToggle={menuToggleHandler} />
